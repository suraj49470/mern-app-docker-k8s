version: '3.8'

services:
  mongo1:
    image: mongo
    container_name: mongo1
    command: ["chmod", "+x", "initiate_replica_set.sh", "&&", "initiate_replica_set.sh"]
    ports:
      - "27017:27017"
    volumes:
      - ./mongo1/data:/data/db
      - ./initiate_replica_set.sh:/initiate_replica_set.sh
    networks:
      - mongo-net

  mongo2:
    image: mongo
    container_name: mongo2
    command: ["mongod", "--replSet", "rs0", "--port", "27017"]
    ports:
      - "27018:27017"
    volumes:
      - ./mongo2/data:/data/db
    networks:
      - mongo-net

  mongo3:
    image: mongo
    container_name: mongo3
    command: ["mongod", "--replSet", "rs0", "--port", "27017"]
    ports:
      - "27019:27017"
    volumes:
      - ./mongo3/data:/data/db
    networks:
      - mongo-net

networks:
  mongo-net:
